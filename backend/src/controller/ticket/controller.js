/*const baseService = require('../base/service');
const Ticket = require('./model');
const service = baseService(Ticket);

exports.findAll = (req, res, next) => {

    return service.findAll()
        .then( list => res.json(list) );
};
*/